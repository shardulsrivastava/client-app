version: '3.7'

services:
  api:
    container_name: api
    build:
      context: backend/
      dockerfile: Dockerfile
    ports:
      - 8087:8080
  app:
    container_name: app
    build:
      context: frontend/
      dockerfile: Dockerfile
    ports:
      - 8088:80